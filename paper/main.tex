% AKBC Shared Task 2026 (LM-KBC, 5th edition) system description paper.
% Regulations: ACL style, max 6 pages main content, double column,
% single-blind review, OpenReview submission. Deadline: 2026-08-15.
%
% DRAFT STATUS (v4, 2026-08-15; rebased on the v12 gemma-3-27b-pt system
% and paper/paper-memo.md sections 8.1-8.27):
% - Final system: v19, official test All 0.6670 (6th), 27.4B, compliant.
% - Best Mistral-era compliant system: v11, test 0.6247.
% - Claims rest on validation/train; test is reported for submissions only.
\documentclass[11pt]{article}

\usepackage[final]{acl}
\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}

\title{Exemplars Move Retrieval, Instructions Move Rejection:\\
An Anatomy of Inference-Time Interventions for\\
Closed-Book Knowledge Base Construction}

\author{Takashi Sasaki \\
  Acroquest Technology Co., Ltd. \\
  \texttt{sasaki@acroquest.co.jp}}

\begin{document}
\maketitle

\begin{abstract}
We describe our system for the AKBC 2026 shared task on closed-book
knowledge base construction. Our final system is a single pretrained-only
backbone, Gemma-3-27B-pt, queried entirely in raw text-completion
registers with 20 candidates per query, gated relation-specific
aggregation, and no arbiter; it uses 27.4B of the 32B parameter budget
and scores a row-averaged Macro F1 of 0.6670 on the official test set,
6th on the leaderboard. Its
predecessor---two deliberately different prompts over Mistral Small 3.2
24B with a cross-family 8B arbiter, 31.8B in total---scores 0.6247. Most
of the paper presents a mechanism study over three backbones and roughly
forty controlled experiments, organized around three results. First, how
reliably a backbone recalls the value stored for an entity label
dominated every intervention we tried: each backbone change moved the
score more than all our prompt, sampling, and selection experiments
combined, and the winner is a pretrained checkpoint: its
instruction-tuned sibling, queried in the same registers so that only the
weights differ, loses 0.11 on validation. Second,
exemplars move retrieval and instruction text does not---two in-prompt
exemplars demonstrating a verbalized recall move the answer mode by $+6$
validation rows, while the same request as an instruction moves nothing,
and replacing synthetic exemplars with real training answers adds a
further $+2.1$ points---but what instructions do move is \emph{rejection}:
a register that writes a one-line biography before naming a city lets the
model decline rows it cannot answer, raising that relation from 0.55 to
0.61 on validation and reproducing on train and across seeds. Third,
validation gains transferred to the hidden test set when the change was
gated, when the gate consulted information from outside the model, and
when the gain was stable across sampling seeds; each criterion has a
counterexample that cost us test rows.
\end{abstract}

\section{Introduction}

The LM-KBC shared task series \citep{lmkbc2023,lmkbc2024} probes how much
factual knowledge language models can turn into knowledge base entries:
given a subject and a relation, a system must output \emph{all} correct
object values---possibly none, one, or hundreds---under closed-book
conditions: no retrieval, no external corpora, no fine-tuning, and at most
32B neural parameters at inference.

Under these conditions the only controls are at inference time: model
choice, prompts, sampling, and aggregation over samples. Our development
proceeded in three stages: a Qwen3.5-27B thinking system (test 0.571), a
Mistral Small 3.2 system with a second prompt and a cross-family 8B
arbiter (0.6247), and a single pretrained-only Gemma-3-27B backbone in
completion registers (0.6670). Alongside the system we report the
controlled experiments behind each decision. Those experiments are
measured on validation and, where a threshold had to be chosen, on the
training split; the hidden test set enters only as the outcome of
submissions, which lets us ask separately which validation gains
survived it.

\begin{enumerate}
  \item \textbf{Backbone recall reliability dominates.} A 12-row probe
    (30 GPU-minutes per model) ranked backbones consistently with
    downstream quality in every deployment decision we evaluated, and
    both backbone changes beat all our prompt and selection work combined
    (Sec.~\ref{sec:screening}).
  \item \textbf{Answers commit early} to the first concrete value they
    generate (Sec.~\ref{sec:anchor}), across backbones.
  \item \textbf{Exemplar style moves retrieval; instruction text does
    not} (Sec.~\ref{sec:exemplar}); instructions move rejection. The
    effect is conditional on recall reliability and on post-training
    (Sec.~\ref{sec:conditions}).
  \item \textbf{Within one model we found no usable selection signal};
    across prompts and families we did (Sec.~\ref{sec:ensemble}).
  \item \textbf{Validation gains transfer when the change is gated and
    the gate looks outside the model} (Sec.~\ref{sec:transfer}).
  \item \textbf{The residual errors are consistent with incorrect values
    learned from training data}, and are associated with the
    instruct-plus-chat setting (Sec.~\ref{sec:associations}).
\end{enumerate}

\section{Task and Constraints}

The 2026 task covers six relations over 475 validation and 475 test
subject--relation pairs: two set-valued relations that may be empty
(\texttt{countryLandBordersCountry},
\texttt{companyTradesAtStockExchange}), one single-valued string relation
that may be empty (\texttt{personHasCityOfDeath}), one large-set relation
(\texttt{awardWonBy}), and two numeric relations (\texttt{hasArea},
\texttt{hasCapacity}) scored at 5\% relative tolerance. Gold values come
from Wikidata (areas \texttt{P2046}, capacities \texttt{P1083}), with
alias lists accepted for strings. We report the official ``All
Relations'' score throughout: a Macro F1 averaged over \emph{rows}, not
relations, so the four 100-row relations dominate it. Systems are
closed-book and capped at 32B inference parameters.

\section{System Description}
\label{sec:system}

\subsection{Final system}

The final system generates with Gemma-3-27B-pt (the pretrained
checkpoint, Q8\_0, served by \texttt{llama.cpp} at a pinned revision),
one model and no arbiter, 27.4B of the 32B budget. Every relation is
queried in a \emph{raw text-completion register}---a record- or
infobox-style prefix that the model continues, with no chat template and
no instruction turn---with 20 candidates per query at temperature 0.6,
top-$p$ 0.95 and deterministic per-candidate seeds, as in the earlier
systems. Two registers depart from a bare record. \texttt{hasArea} and
\texttt{hasCapacity} carry four fixed exemplars drawn from the
\emph{training} split rather than written by us
(Sec.~\ref{sec:exemplar}); \texttt{personHasCityOfDeath} writes a
one-line biography before the answer field (Sec.~\ref{sec:rejection}).
Neither changes generation or aggregation otherwise.

Aggregation is relation-specific and, in every case, \emph{gated}: a rule
applies only where an explicit support condition holds and falls back to
a default otherwise. Before voting, string surfaces are folded onto
public gold entity classes with an alias graph built from the released
train and validation answers, using English-label matches only
(validation $0.658\rightarrow0.663$, driven by company). Numeric
candidates are clustered at 5\% relative tolerance and
\texttt{dominant\_cluster} predicts the median of the best-supported
cluster. \texttt{personHasCityOfDeath} takes a surface majority with a
minimum-vote gate of 14 of 20, predicting empty below it.
\texttt{companyTradesAtStockExchange} folds surface variants---case
variants and acronyms that expand to a co-occurring long form (NYSE
$\rightarrow$ New York Stock Exchange)---into a single count, repairing
vote splits that would drop majority answers below its frequency
threshold ($\ge0.5$), and abstains on an empty majority. \texttt{countryLandBordersCountry} and \texttt{awardWonBy} use
recall-oriented thresholds ($\ge0.3$, $\ge0.1$) with the same abstention.

\textbf{Reproducibility.} Our submissions appear on the official
Codabench leaderboard under the team name \texttt{acro-sasaki}. Code,
configurations, builders, the step-by-step recipe
(\texttt{docs/V4\_REPRODUCE.md}), and per-experiment records are at
\url{https://github.com/acrosasaki-rgb/llm-knowledge-task-2026}. Model
revisions, container digests, and the dataset commit are pinned; each
prediction file is a deterministic function of persisted candidate sets
and a builder script (SHA-256 verified).

\subsection{Predecessor system}

Most experiments in Sec.~\ref{sec:anatomy} come from the Mistral-era
system that preceded it. It samples 20 candidates per prompt from Mistral
Small 3.2 24B under \emph{two} prompts of deliberately different style
for \texttt{hasArea}: a \textbf{recall prompt} with no few-shot examples
but two in-prompt exemplars demonstrating a labeled recall format
(\texttt{Entity:}, \texttt{Memory:}\ ``I recall that the Wikipedia
infobox lists X's total area as Y'', \texttt{Unit:}, \texttt{Answer:}),
and a \textbf{grounding prompt} with five bare-JSON few-shot examples
naming the recall target (Wikidata \texttt{P2046}/\texttt{P1083}). When
the two disagree by more than 5\%, one aggregated arbiter decision---the
mode over 20 samples of an unrelated-family Llama-3.1-8B---breaks the
tie, switching only if the arbiter matches the secondary answer and not
the primary one. The arbiter never introduces a value neither prompt
proposed, but an incorrect switch can still lose a row. Total:
$24\text{B}+8\text{B}\approx31.8\text{B}$.

\subsection{Results}

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lc}
\toprule
System & test \\
\midrule
Qwen3.5-27B thinking & 0.571 \\
\midrule
Mistral 3.2 grounding & 0.595 \\
v3: + recall prompt, area arbitration, merge & 0.604 \\
v4: + capacity arbitration & 0.606 \\
v5: + acronym merging & 0.610 \\
v7: + city merge & 0.608 \\
v8: re-keying$^{\ast}$ & 0.625 \\
v10: + tolerance rescoring & 0.623 \\
v11: $-$ rescoring & 0.625 \\
\midrule
v12: Gemma-3-27B-pt & 0.649 \\
v13: + alias graph & 0.648 \\
v16: + train exemplars & 0.659 \\
v18: + city vote gate & 0.663 \\
v19: + biography register & \textbf{0.667} \\
\bottomrule
\end{tabular}
\caption{Official test scores of our submissions (row-averaged Macro F1,
three decimals). Each row is its predecessor plus the named change, so
the column doubles as a sequence of isolated ablations on the hidden set
(Sec.~\ref{sec:transfer}). Validation scores are not comparable across
the whole table---the August release re-keyed the gold---so they are
reported per experiment in Sec.~\ref{sec:anatomy} instead. The official
baseline scores 0.313 on validation. $^{\ast}$Following that release,
which renamed 15 test subjects and dropped two rows
(477$\rightarrow$475); v3--v7 score the renamed rows as zero. Versions
differing only in variants we did not deploy (v9, v14--v15, v17) are
omitted for space.}
\label{tab:main}
\end{table}

Four contributions are separable on test: the Qwen$\rightarrow$Mistral
backbone change ($+0.024$, rest of the pipeline unchanged), the
Mistral-era pipeline interventions ($+0.015$, v3$\rightarrow$v5), the
Mistral$\rightarrow$Gemma-pt backbone change ($+0.025$,
v11$\rightarrow$v12), and the two register changes of
Sec.~\ref{sec:exemplar} and Sec.~\ref{sec:rejection} ($+0.018$,
v13$\rightarrow$v19). A further $+0.015$ came from following the dataset
re-keying, which corrects what is scored rather than improving the
system. Per-relation test scores for the final system are: borders 0.960,
company 0.832, area 0.820, city 0.530, award 0.336, capacity 0.316. The
backbone change is what moved company ($0.778\rightarrow0.832$) and award
($0.279\rightarrow0.336$); the register changes moved capacity
($0.276\rightarrow0.316$) and city ($0.490\rightarrow0.530$).

\section{Anatomy of Inference-Time Interventions}
\label{sec:anatomy}

All experiments change one stated condition; numeric testbeds are
\texttt{hasArea} and \texttt{hasCapacity} (100 validation rows $\times$
20 candidates per prompt). \textbf{Oracle} is the number of rows whose
candidate set contains at least one within-tolerance value; the
\textbf{mode} row count is what \texttt{dominant\_cluster} selects. The
\textbf{anchor} is the first concrete value for the queried quantity in a
candidate.

\subsection{Backbone Recall Reliability Dominates}
\label{sec:screening}

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrr}
\toprule
Model (12-row probe) & any & mode \\
\midrule
Mistral Small 3.2 Instruct & 11 & 8 \\
Mistral Small 3.1 Base & 10 & 7 \\
Mistral Small 3.1 Instruct & 9 & 4 \\
Llama-3.1-8B Instruct & 7 & 4 \\
Aya Expanse 32B & 3 & 1 \\
Qwen3-8B & 1 & 0 \\
\bottomrule
\end{tabular}
\caption{The recall probe: 12 \texttt{hasArea} rows stratifying the
Qwen-era system's failures, 20 candidates each; \emph{any} = rows with a
correct candidate, \emph{mode} = rows whose modal cluster is correct.
Qwen3.5/3.6-27B, Gemma~4 31B, GLM-Z1-32B and llm-jp-4-32b (not shown) sit
between Llama-8B and Aya. It costs $\sim$30 GPU-minutes per model.}
\label{tab:probe}
\end{table}

Our Qwen-era system plateaued after nine intervention experiments
(prompts, temperature, output routes, thinking budgets, judge models,
log-probability confidence) whose combined deployable gain was under
$+0.01$. A 12-row probe of its wrong rows across candidate models
(Table~\ref{tab:probe}) showed Mistral Small 3.2 answering 11 of the 12
somewhere in its candidate set; regenerating the pipeline on Mistral with
nothing else changed moved test 0.571$\rightarrow$0.595.

The second change was larger and reversed an earlier decision. Gemma~4
31B Instruct, queried through its chat template, had the weakest
\texttt{hasArea} oracle we measured (43/100) and we closed that lineage
early. Probing the \emph{pretrained} Gemma-3-27B checkpoint in a
completion format instead gave the best profile of any configuration we
tested---oracle 94, mode 84 on \texttt{hasArea}, borders 0.986---and a
system built on it took test $0.625\rightarrow0.649$. It also supplies
values no prompt reached on Mistral: secondary stock listings that five
Mistral prompt variants returned with zero votes (\v{C}EZ on Warsaw,
Questerre on Oslo, Aisin on Nagoya) appear with 7--13 votes out of 20.
This was the dominant empirical factor in our experiments: closed-book
quality is set by which label-to-value pairings pretraining left intact
and by whether the prompting format can reach them.

\subsection{Answers Commit Early}
\label{sec:anchor}

In persisted reasoning traces of the Qwen-era system, the answer value
first appears at a median 17\% of the trace, in 95--98\% of candidates
inside a retrieval template (``Commonly cited: X''); the remaining 80\%
of the trace never dislodges it. The verification text that follows is
consistent with post-hoc justification rather than independent checking:
fabricated Wikidata Q-IDs and cross-language ``confirmations'' agree with
whatever the anchor was, and wrong candidates carry \emph{more} of this
apparatus than correct ones (6.9 vs.\ 4.2 source mentions). Early
commitment replicates on Gemma~4 31B and Qwen3.6; the confabulated
verification does not. Extended thinking is thus an \emph{exploration}
aid: it raises the oracle where recall is unreliable (Qwen $+9$ rows,
Gemma $+7$) and has no measurable effect on Mistral or Magistral.

\subsection{Exemplar Style Moves Retrieval; Instruction Text Does Not}
\label{sec:exemplar}

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrr}
\toprule
\texttt{hasArea} prompt & \multicolumn{2}{c}{Few-shot examples} \\
(Mistral 3.2) & bare-JSON $\times$5 & none \\
\midrule
grounding instruction & 71\;(o.\,89) & \textbf{41}\;(o.\,59) \\
recall-format text & 72\;(o.\,83) & \textbf{78}\;(o.\,84--89) \\
\bottomrule
\end{tabular}
\caption{The $2{\times}2$ comparison: mode-correct rows out of 100
(oracle in parentheses). The recall-format text carries two exemplars
inside the instruction, so the bottom-right cell keeps two exemplars
while the top-right has \emph{none} and collapses to 41. At fixed example
style the instruction moves the result by $\pm1$; the example style moves
it by $+6$ / $-30$.}
\label{tab:grid}
\end{table}

The single largest prompt effect we found---\texttt{hasArea} mode
$72\rightarrow78$, transferring to test as $0.70\rightarrow0.75$---looked
at first like an instruction effect: a step-structured expert prompt,
adapted from a style published by another participant, beat our grounding
instruction by 6 rows. The $2{\times}2$ comparison
(Table~\ref{tab:grid}) shows the instruction text is nearly inert. What
matters is the \emph{exemplars}: with two in-prompt
exemplars demonstrating a one-sentence verbalized recall (``I recall that
the Wikipedia infobox lists Luxembourg's total area as 2586.4\,km$^2$''),
99\% of candidates reproduce the verbalization and the mode moves $+6$;
with five bare-JSON few-shot exemplars, 0\% verbalize---the same
procedure requested as instruction text is silently ignored---and the
mode does not move. Iterating the wording (persona, fixed phrase,
provenance vocabulary) changed nothing, while reproducing the labeled
structure with our own labels recovered the full effect.

The final system carries this one step further. Replacing our
hand-written exemplars with four answers taken verbatim from the
\emph{training} split---same register, same generation, same
aggregation, only the examples change---moves validation from 0.665 to
0.686, with \texttt{hasArea} $0.84\rightarrow0.88$ and
\texttt{hasCapacity} $0.309\rightarrow0.361$. The gain holds across three
sampling seeds (area 0.88/0.89/0.87, capacity 0.361/0.351/0.361), and no
exemplar subject occurs in validation or test.

\subsection{Instructions Move Rejection}
\label{sec:rejection}

Instruction text was not inert everywhere: it moved the
\emph{discarding} of already-generated candidates. Verifying each
\texttt{awardWonBy} candidate with three yes/no votes and dropping only
unanimous-no candidates raised precision $0.241\rightarrow0.280$ and F1
$0.172\rightarrow0.186$; the yes votes carried no information, so the
usable signal was one-sided. A digit-check step inside an otherwise inert
step-by-step procedure likewise cut votes for order-of-magnitude twin
values from 38 to 32 with the oracle unchanged.

The clearest case is \texttt{personHasCityOfDeath}, the one relation
whose answer may legitimately be empty. Asking the register to write a
one-line biography before the answer field---\texttt{About:} then
\texttt{City of death:}---raises it from 0.55 to 0.61 on validation
against the same aggregation, reproduces on the training split
($0.52\rightarrow0.55$) and on a second seed (0.60), and beats every
other register we wrote. The traces show why: the model states ``he is
still alive'' for living subjects, converting five wrong fills into
abstentions, and recalls six further correct cities. The same register
applied to \texttt{hasArea}, \texttt{hasCapacity} and
\texttt{companyTradesAtStockExchange} changes nothing
($0\ldots-2$)---those relations do not ask whether to answer at all.

Style, however, only moves where the model operates, not how well it
separates. Compared at matched numbers of filled rows, the biography
register dominates every other register we wrote (at 61 fills, 34 correct
against 31), while styles that bias toward ``alive'' abstain more but
discard correct answers at the same rate as wrong ones. Fifteen further
gates for this relation, from obituary-style vetoes to
internal-activation familiarity probes, moved validation by at most two
rows.

\subsection{The Exemplar Effect Is Conditional}
\label{sec:conditions}

Verbalized recall is a \emph{focusing} intervention: it sharpens the
anchor distribution around the most frequently recalled value (oracle
$89\rightarrow84$ as the mode improves), so its sign follows how reliable
recall already is, in mirror image to thinking. Thinking explores,
gaining $+9$ rows where recall is unreliable (Qwen) and nothing where it
is reliable (Mistral); verbalized recall does the opposite, zero or
negative on Qwen and on Mistral's weakest relation (\texttt{hasCapacity}
oracle $60\rightarrow47$) but $+6$ on \texttt{hasArea}. In Magistral
(Mistral's RL-tuned sibling) it disappears: the verbalized form is copied
at 99.9\% compliance in three different channels, yet the anchor
distribution does not move in any of them (70--72), suggesting that RL
post-training may decouple response style from retrieval.

\subsection{We Find No Usable Within-Model Selection Signal in the
Features We Tested}
\label{sec:ensemble}

Within one model's candidate set, minority correct values were
indistinguishable from minority wrong ones on every feature we measured
(votes, dispersion, trace features, log-probabilities; base rate 8\%),
which is why judges, confidence weighting, and consensus schemes failed
in our experiments. What did extract signal is \emph{disagreement between
prompts of different style}: on the $\sim$25 rows where the recall and
grounding prompts disagree by more than 5\%, one aggregated decision from
an unrelated-family 8B model resolves the tie, giving \texttt{hasArea}
$78\rightarrow80$ and \texttt{hasCapacity} $26\rightarrow28$ on
validation; of the four \texttt{hasArea} rows it changed, three were
corrected and one harmed. Controls locate the conditions: same-style
prompt pairs produce too little disagreement to arbitrate; promoting
minority clusters by the arbiter's mode is \emph{negative}---an 8B cannot
overrule a 24B mode, only break ties; and a weak arbiter (Ministral-8B,
same family, mode accuracy 23 vs.\ Llama's 53 of 100) gains nothing. The
isolated v3$\rightarrow$v4 ablation confirms the transfer on test:
capacity F1 $0.204\rightarrow0.214$.

\subsection{Which Validation Gains Transfer}
\label{sec:transfer}

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrc}
\toprule
Component & val & test & transfers \\
\midrule
recall exemplars (\S\ref{sec:exemplar}) & $+6$ & $+5$ & \checkmark \\
company folding (\S\ref{sec:system}) & $+1$ & $+2$ & \checkmark \\
8B arbitration (\S\ref{sec:ensemble}) & $+2$ & $+1$ & \checkmark \\
train exemplars: area, capacity & $+8$ & $+5$ & \checkmark \\
\midrule
capacity prompt (best of 6) & $+2$ & $-2$ & $\times$ \\
second arbiter (increment) & $+1$ & $0$ & $\times$ \\
tolerance rescoring & $+2$ & $-1$ & $\times$ \\
train exemplars: city & $+4$ & $-5$ & $\times$ \\
city two-stage merge & $+11$ & $-1$ & $\times$ \\
\bottomrule
\end{tabular}
\caption{Row-level validation and test deltas for deployed components,
from the isolated ablations of Table~\ref{tab:main}. The split follows
neither effect size (our largest validation gain transferred worst) nor
the presence of a mechanism-level rationale (the rescoring has one).}
\label{tab:transfer}
\end{table}

Our submissions give per-component test measurements
(Table~\ref{tab:transfer}). The components that transferred share two
properties: each is \emph{gated}, touching only rows that meet an
explicit condition, and each gate consults information from outside the
model---an unrelated-family vote, or the gold alias structure that makes
two surface forms one count. The exemplar effect is the apparent
exception: it changes what the backbone retrieves rather than
adjudicating between candidates.

The components that did not transfer are fits to validation row
distributions: the best of six capacity configurations (winner's curse),
an increment to an already-saturated arbiter, and a city pipeline whose
$+11$-row gain rested on the rate at which split-vote rows happen to be
correct-mode. The most instructive case is a tolerance-geometry rescoring
we deployed in v9--v10. Two values whose ratio is below
$1.05/0.95\approx1.105$ admit a single prediction correct for either, so
each answer can be shifted inside its own $\pm5\%$ window to also cover a
runner-up cluster. It follows from the metric geometry, has no tuned
constants, and gained $+1$ to $+2$ rows across every prompt pairing and
arbiter seed we tested, yet it cost a row on test (\texttt{hasArea}
$0.800\rightarrow0.790$), because it moves \emph{every} row rather than
only rows meeting a condition; we removed it in v11. Later gated experiments failed the
second criterion the same way: certificates computed from the same
model's internal consistency (unit agreement, answer-blind priming)
admitted confident errors and lost rows. A third predictor appeared late. When we
replaced our exemplars with training answers, the same change helped
\texttt{hasArea} and \texttt{hasCapacity} on all three seeds we ran and
helped \texttt{personHasCityOfDeath} on one seed only (0.56/0.50/0.49);
we deployed all three, and on test the two seed-stable relations
transferred ($+4$ capacity rows) while the seed-unstable one cost five
rows. We now treat single-seed differences below about four rows on a
100-row relation as unusable. A 100-row validation set may not reliably
expose these failures; in our experiments, whether a component is gated,
whether its gate looks outside the model, and whether its gain survives
reseeding predicted transfer better than effect size or the presence of a
mechanism-level rationale.

\subsection{Residual Errors Look Like Values Learned From the Corpus}
\label{sec:associations}

The residual errors we examined are not reasoning failures. Corfu (gold
626\,km$^2$) is answered as 592.9 by every Mistral-family instruct
configuration, 593 by Qwen3.5, and 580 by Gemma: stable, model-specific
wrong values that survive every prompt. Three observations support the
hypothesis that they were learned from training data. First,
\emph{turnover across generations}: Qwen3.6 repairs 7 of Qwen3.5's wrong
rows and breaks 11 new ones with nothing else changed, and the union
oracle reaches 89/100. Second, \emph{unrelated training lineages solve
mainstream-broken rows}: llm-jp-4-32b and Aya Expanse are far weaker
overall yet each partially solves Corfu, which no mainstream-corpus model
in our set solves. Third, \emph{instruction tuning costs recall}: under
raw completion Mistral 3.1 Base scores 10/12-any against its own
instruct's 9, and the gap replicates on Llama-3.1-8B (mode accuracy 60
vs.\ 53 of 100).

The last comparison is the one our final system rests on, so we ran it
without the register confound: the instruction-tuned sibling of our
backbone, queried in the \emph{same} completion registers with the same
sampling and aggregation, scores 0.553 on validation against 0.665 for
the pretrained checkpoint (\texttt{hasArea} 0.61 vs.\ 0.84,
\texttt{hasCapacity} 0.24 vs.\ 0.31, \texttt{personHasCityOfDeath} 0.38
vs.\ 0.52), reproduced on two seeds. Since weights are the only
difference, the loss belongs to instruction tuning itself rather than to
the chat template, at least in this lineage---and the abstention
behaviour that makes the biography register work
(Sec.~\ref{sec:rejection}) is absent in the tuned sibling, which
fabricates instead. What all of this establishes is that the accessible
ceiling is a property of the checkpoint, not of prompt engineering within
one setting.

\section{Related Work}

Probing factual knowledge in LMs goes back to LAMA
\citep{petroni2019lama}; the LM-KBC series \citep{lmkbc2023,lmkbc2024}
extends it to KB construction with unconstrained cardinalities.
Self-consistency \citep{wang2023selfconsistency} aggregates sampled
answers by majority vote and ReWiSe \citep{albertroulhac2025rewise} tuned
it relation-wise; we extend the line to empty answer sets, numeric
tolerance, surface-form folding, and cross-family arbitration.
\citet{sprague2025cot} find chain-of-thought helps reasoning but not
knowledge recall; we refine this for recall tasks, where exemplars move
retrieval although instructions do not, and where a chain-of-thought
register helps exactly one relation---the one that must decide whether to
answer. \citet{ma2026parametric} report prompting moves parametric
knowledge access by 1--2\% and RL fine-tuning by $\sim$10\%; our exemplar
effect sits between, and fine-tuning is prohibited here.
\citet{numbers2025know} find probe-recoverable numeric knowledge exceeds
what models verbalize, one reading of our oracle--mode gaps;
\citet{kalyan2021fermi} and \citet{park2022measurements} document the
magnitude and unit fragilities our components target; and grounding
instructions instantiate ``According to\ldots'' cues
\citep{weller2024according}.

\section{Conclusion}

Our final system is a single pretrained-only 27.4B backbone queried in
completion registers with gated aggregation (test 0.6670, 6th on the
leaderboard); the two-prompt, arbiter-ensembled 24B system it replaced
scores 0.6247.
The most reliable finding behind that progression is that backbone recall
reliability, together with a prompting format that can reach it,
outweighed every prompt-level intervention we tested, and that a
half-hour probe ranked candidates well enough to act on.

Two rules earned their place. Choose exemplars, not instructions, to
change what a model retrieves; instruction text in our experiments moved
only the rejection of already-generated candidates. And before deploying
what a small validation set endorses, ask whether the component is gated
and whether its gate consults anything outside the model---those that
failed this test cost us rows even with a clean mechanism-level
rationale. Both rules are bounded by what we measured: three backbones
and one task edition.

\section*{Limitations}

The mechanism findings rest on two model families in depth (Qwen,
Mistral), with seed replications only for the exemplar, arbitration and
register results. The final backbone is the least ablated configuration
here: its per-component contributions are not separated on test. The
12-row probe is stratified on one system's failures and its ranking is
validated on the decisions we took, not a held-out model set. Counts on
100-row relations carry $\pm1$--$2$ row noise, which bounds several
negative results. The instruct-versus-pretrained control of
Sec.~\ref{sec:associations} holds the register fixed, but our earlier
Mistral and Llama comparisons do not, and the account of residual errors
is observational. Thresholds are tuned on validation or train, and the
transfer criterion of Sec.~\ref{sec:transfer} comes from one task
edition and from a single system lineage, so it should be read as a
working heuristic rather than a validated rule.

\bibliography{references}

\end{document}
